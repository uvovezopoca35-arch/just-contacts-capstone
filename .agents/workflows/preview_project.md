---
description: Preview Just Contacts Local Server
---
# Preview Local Server

Start the local development server for testing.

1. Ensure you have installed the project dependencies (`npm install`).
2. Run the Next.js development server.
// turbo
3. Run `npm run dev`
4. Open your browser and navigate to `http://localhost:9002/`.
